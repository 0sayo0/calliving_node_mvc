header(class="bg-teal-600 p-4 block shadow-md shadow-rose-500")
  div(class="container mx-auto flex flex-col md:flex-row justify-between items-center")
    a(class="flex items-center justify-center gap-3" href="/")
      img(src="/img/calliving-removebg-white.png" class="w-[50px]" alt="Calliving logo")
      h1.text-2xl.text-white.font-extrabold.text-center Calliving
        span.font-normal Inmuebles

    nav(class="my-5 text-sm flex flex-col md:flex-row justify-center items-center gap-6 md:flex md:gap-6 md:items-center font-bold text-white")
      a(href="/my-properties") Mis propiedades
      a(href="/my-profile") Mi perfil

      form(action="/auth/logout" method="POST")
        input(type="hidden" name="_csrf" value=csrfToken)
        input(type="submit" class="bg-teal-800 py-2 px-8 rounded-lg cursor-pointer mt-2" value="Cerrar sesión")
